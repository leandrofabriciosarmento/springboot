apiVersion: v1
kind: Pod
metadata:
  name: pod-leandro-springboot
  labels:
    app: leandro-springboot
spec:
  containers:
    - name: container-leandro-springboot
      image: leandrofabriciosarmento/springboot:1.0
      ports:
        - containerPort: 8080
      envFrom:
        - configMapRef:
            name: configmap-leandro-springboot